* make sure core has all real logic
* make sure core has all real configuration
* move builtin into its own package?
* fix auth
* testing
* update docs
* rename ClientCommand
* make configuration reloadable (signal handling, diff on connections, etc),
* make storage do reconnection, and use a central channel instead of central
  goroutine
* make pubsub more efficient
* address all TODO notes in the code
